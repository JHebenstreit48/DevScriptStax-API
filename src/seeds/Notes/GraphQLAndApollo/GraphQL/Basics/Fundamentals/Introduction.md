## GraphQL Introduction

---

### Overview

GraphQL is a query language for APIs and a runtime for fulfilling those queries using your existing data.

It allows clients to request only the data they need, which helps reduce over-fetching and under-fetching.

---

### Why GraphQL Exists

<div class="fullWidthBullet">

🔹 Clients can request exactly the fields they need  
🔹 A single request can fetch related data in one response  
🔹 The schema acts like a typed contract between client and server  

</div>

---

### Debugging (Quick Note)

When debugging GraphQL requests from a web app, browser dev tools can help.

<div class="fullWidthBullet">

🔹 Use the Network tab to inspect the request payload  
🔹 Verify returned data and error messages in the response  
🔹 Confirm headers and status codes when diagnosing failures  

</div>